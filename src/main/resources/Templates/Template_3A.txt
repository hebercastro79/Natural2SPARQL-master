SELECT DISTINCT ?empresa (SAMPLE(STR(?setorLabel)) as ?sl)
WHERE {
  ?empresa stock:atuaEm ?setorUri .
  ?setorUri rdfs:label ?setorLabel .
  FILTER(REGEX(STR(?setorLabel), "energia.*el[eé]trica", "i")) 
}
GROUP BY ?empresa 
LIMIT 100